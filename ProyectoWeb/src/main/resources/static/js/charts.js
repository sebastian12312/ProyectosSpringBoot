var datos = {
    labels: ['Categoría 1', 'Categoría 2', 'Categoría 3'],
    datasets: [{
      data: [30, 50, 20], // Porcentajes o valores de cada categoría
      backgroundColor: ['#ff6384', '#36a2eb', '#ffce56'], // Colores de las secciones
    }]
  };

  // Configuración del gráfico
  var opciones = {
    responsive: true,
    maintainAspectRatio: false,
  };

  // Obtener el contexto del canvas y dibujar el gráfico de pastel
  var ctx = document.getElementById('miGrafico').getContext('2d');
  var miGrafico = new Chart(ctx, {
    type: 'pie',
    data: datos,
    options: opciones
  });
  var datos = {
    labels: ['Categoría 1', 'Categoría 2', 'Categoría 3'],
    datasets: [{
      data: [30, 50, 20], // Porcentajes o valores de cada categoría
      backgroundColor: ['#ff6384', '#36a2eb', '#ffce56'], // Colores de las secciones
    }]
  };

  // Configuración del gráfico
  var opciones = {
    responsive: true,
    maintainAspectRatio: false,
  };

  // Obtener el contexto del canvas y dibujar el gráfico de pastel
  var ctx = document.getElementById('miGrafico1').getContext('2d');
  var miGrafico = new Chart(ctx, {
    type: 'pie',
    data: datos,
    options: opciones
  });
  var datos = {
    labels: ['Categoría 1', 'Categoría 2', 'Categoría 3'],
    datasets: [{
      data: [30, 50, 20], // Porcentajes o valores de cada categoría
      backgroundColor: ['#ff6384', '#36a2eb', '#ffce56'], // Colores de las secciones
    }]
  };

  // Configuración del gráfico
  var opciones = {
    responsive: true,
    maintainAspectRatio: false,
  };

  // Obtener el contexto del canvas y dibujar el gráfico de pastel
  var ctx = document.getElementById('miGrafico2').getContext('2d');
  var miGrafico = new Chart(ctx, {
    type: 'pie',
    data: datos,
    options: opciones
  });
  var datos = {
    labels: ['Categoría 1', 'Categoría 2', 'Categoría 3'],
    datasets: [{
      data: [30, 50, 20], // Porcentajes o valores de cada categoría
      backgroundColor: ['#ff6384', '#36a2eb', '#ffce56'], // Colores de las secciones
    }]
  };

  // Configuración del gráfico
  var opciones = {
    responsive: true,
    maintainAspectRatio: false,
  };

  // Obtener el contexto del canvas y dibujar el gráfico de pastel
  var ctx = document.getElementById('miGrafico3').getContext('2d');
  var miGrafico = new Chart(ctx, {
    type: 'pie',
    data: datos,
    options: opciones
  });